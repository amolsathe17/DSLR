# Project-Scoped Rules

- **Automated Pushing**: After verifying that there are zero compilation/build errors following any code modification, always stage all changed files, commit them, and execute `git push origin main` directly without asking for confirmation.
